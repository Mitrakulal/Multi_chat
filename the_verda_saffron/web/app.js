const cfg=window.HOTEL_CONFIG;const messages=document.querySelector('#messages');const form=document.querySelector('#chat-form');const input=document.querySelector('#question');const suggestions=document.querySelector('#suggestions');const meta=document.querySelector('#meta');
const esc=s=>String(s).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const add=(role,text)=>{const row=document.createElement('div');row.className='message '+role;row.innerHTML='<div class="bubble">'+esc(text).replace(/\\n/g,'<br>')+'</div>';messages.appendChild(row);messages.scrollTop=messages.scrollHeight;};
const showSources=sources=>{meta.innerHTML=(sources||[]).map(s=>`<div class="source"><strong>${esc(s.title)}</strong><br><a href="${esc(s.url)}" target="_blank" rel="noreferrer">${esc(s.url)}</a></div>`).join('')||'<div class="muted">No retrieved source metadata.</div>';};
const ask=async q=>{if(!q)return;add('user',q);input.value='';add('assistant','Checking the approved hotel information…');try{const r=await fetch('api/ask',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q})});if(!r.ok)throw new Error('API unavailable');const d=await r.json();messages.lastElementChild.remove();add('assistant',d.answer);showSources(d.sources)}catch(e){messages.lastElementChild.remove();add('assistant','This static demo is ready to connect to the hotel RAG API. Run the included backend adapter or open the folder README for local setup.');showSources([{title:'Knowledge base',url:cfg.url||'verification-needed'}])}};
(cfg.examples||[]).forEach(q=>{const b=document.createElement('button');b.type='button';b.textContent=q;b.onclick=()=>ask(q);suggestions.appendChild(b)});meta.innerHTML=`<div class="source"><strong>Source status:</strong> ${cfg.verification_needed?'Verification needed — replace the URL and approve the content before production.':'Official website pages bundled where accessible.'}</div><div class="source"><strong>Website:</strong> ${cfg.url?`<a href="${esc(cfg.url)}" target="_blank" rel="noreferrer">${esc(cfg.url)}</a>`:'No verified official URL in the lead report.'}</div>`;form.addEventListener('submit',e=>{e.preventDefault();ask(input.value.trim())});
